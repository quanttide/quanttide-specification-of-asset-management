# 数字资产发现

系统执行发现活动时，应遵循以下严格的先后顺序：

1. 加载 (Load)：获取 `scope` 定义的物理路径（默认 `.`）。
2. 剪枝 (Prune)：根据 `filters.excludes`进行深度优先过滤。如果目录被排除，系统将不再递归其内容，从而极大提升I/O效率。
3. 采样 (Sample)：定位到物理文件后，开始遍历 `maps` 规则。
4. 映射 (Map)：将物理路径与 `match` 模式匹配。若命中，则根据 assign 执行赋值，生成资产草稿。
5. 产出 (Emit)：输出一份符合“资产字段”定义的逻辑实体列表，准备进入“注册”环节。
